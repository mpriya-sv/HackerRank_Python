students = []
for _ in range(int(input())):
    name = input()
    score = float(input())
    students.append([name, score])
sort = sorted(students, key=lambda x: x[1]) 
second_lowest_score = sorted(list(set(score for name, score in sort)), reverse = True)[-2]
print('\n'.join([i for i, j in sorted(sort) if j == second_lowest_score]))
